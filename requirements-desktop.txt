# Desktop runtime dependencies installed by desktop_launcher.ps1.
# PyTorch and TorchAudio intentionally live in installer_backends.json so pip
# cannot replace the selected CUDA, ROCm, XPU, or CPU build.
transformers>=5.3.0
accelerate
pydub
gradio
tensorboardX
webdataset
numpy
soundfile
librosa
wxPython>=4.2.2
sounddevice>=0.5
hatchling
editables
